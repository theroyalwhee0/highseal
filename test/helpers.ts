/**
 * Create an array of given length and fill each position with that value.
 * @param length The length.
 * @returns The created array.
 */
export function filledArrayOf(length: number) {
    return Array(length).fill(length);
}